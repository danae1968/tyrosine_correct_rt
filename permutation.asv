
trialsNumber=[1:12];
types=3;
blocks=1;

sets=[1:4]';
setsizeO=repmat(sets,[length(trialsNumber)/length(sets),blocks])


Ignore=zeros(length(trialsNumber)/types,blocks); 
No_Inter=ones(length(trialsNumber)/types,blocks);
Update=ones(length(trialsNumber)/types,blocks)+1;
trialTypesO=[Ignore;No_Inter;Update]

trials=struct();

index=randperm(12)'

trialTypes=trialTypesO(index)
setsize=setsizeO(index)


